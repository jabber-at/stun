Description: remove deps in rebar.config
 Without this patch, rebar would download and compile the listed deps
 at compile time.
Author: Philipp Huebner <debalance@debian.org>

Index: erlang-p1-stun/rebar.config
===================================================================
--- erlang-p1-stun.orig/rebar.config
+++ erlang-p1-stun/rebar.config
@@ -1,7 +1,5 @@
 {erl_opts, [debug_info, {i, "include"}]}.
 
-{deps, [{p1_tls, ".*", {git, "https://github.com/processone/tls", {tag, "1.0.0"}}},
-	{p1_utils, ".*", {git, "https://github.com/processone/p1_utils", {tag, "1.0.2"}}}]}.
 
 %% Local Variables:
 %% mode: erlang
